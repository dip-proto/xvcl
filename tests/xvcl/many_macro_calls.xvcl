// Test: More than ten independent macro calls on one line

#inline identity(value)
value
#endinline

sub vcl_recv {
    declare local var.output STRING;
    set var.output = identity("1") identity("2") identity("3") identity("4") identity("5") identity("6") identity("7") identity("8") identity("9") identity("10") identity("11");
}
